on:
  issue_comment:
    types: [created]

jobs:
  test:
    if: contains(github.event.comment.body, '/mandatoryTest')
    runs-on: [self-hosted, macos]

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run mandatory tests
        run: |
          echo "Running mandatory tests..."

        # Add your test commands here
        # For example:
        #   npm install
        #   npm test

